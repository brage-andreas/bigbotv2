module.exports = {
	name: ["say"],
	use: "say tekst",
	about: "Si noe",
	category: "",
	cooldown: 0
}


module.exports.run = (message, args) => {
	const channel = 
	if (!args.length) return message.channel
